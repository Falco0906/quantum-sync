# n8n Webhook Configuration
# Single endpoint for BOTH HR and Finance
REACT_APP_WEBHOOK_URL=https://falco.app.n8n.cloud/webhook/automation

# Optional: Add authentication if needed
# REACT_APP_API_KEY=your-api-key-here

# Optional: API timeout (milliseconds)
# REACT_APP_API_TIMEOUT=30000

# Note: The workflow uses a "type" field to route between HR and Finance processing
# HR request: { "type": "hr", "data": {...} }
# Finance request: { "type": "finance", "data": {...} }